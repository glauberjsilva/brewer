$(function() {
	var modal = $('modalCadastroRapidoEstilo');
	var botaoSalvar = modal.find('.js-modal-cadastro-estilo-salvar-btn');
	var form = modal.on.find('form');
	form.on('Submit', function(event) { e.preventDefault() });
	
)};